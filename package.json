{
    "name": "socket-chat-example",
    "version": "0.0.1",
    "description": "my first socket.io app",
    "dependencies": {
        "express": "^4.17.1",
        "socket.io": "^4.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.4"
    },
    "scripts": {
        "start": "node index.js",
        "start:dev": "nodemon",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
